import type { RouteObject } from 'react-router-dom';

/**
 * Routes for the "specialties" feature. Filled in by the specialties migration wave;
 * see docs/migration/ROUTES.md for the Angular route table to port.
 */
export const specialtiesRoutes: RouteObject[] = [];
